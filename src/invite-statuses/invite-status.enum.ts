export enum InviteStatusEnum {
  created = 1,
  queued = 2,
  sent = 3,
  used = 4,
}

export enum InviteStatusNamesEnum {
  created = 'created',
  queued = 'queued',
  sent = 'sent',
  used = 'used',
}
